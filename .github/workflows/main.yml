name: Python Tests

on: 
  push:
    branches:
      - main
  pull_request:
    types: [opened, assigned]
    branches:
      - main
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Setup python  
      uses: actions/setup-python@v4
      with:
          python-version: '3.10' 

    - name: Install requirement.txxt
      run: |
        python -m pip install
        pip install requirement.txt
